# ⚠️ IMPORTANTE: Nunca commitar .env no repositório!
# Copie este arquivo para .env e preencha os valores reais

# ===== DATABASE =====
# URL do banco de dados MySQL no AlwaysData
# Formato: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://facerec:iqmi8j55PDpHQ@mysql-facerec.alwaysdata.net:3306/facerec_form"

# ===== FRONTEND (Next.js) =====
# URL base da API (usado no client para requisições)
NEXT_PUBLIC_API_BASE_URL="http://localhost:4000"

# ===== BACKEND (Express) =====
# URL do frontend (CORS origin)
WEB_ORIGIN="http://localhost:3000"

# JWT secret para assinar tokens
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="seu-secret-aleatorio-aqui-mude-em-producao"

# Nome do cookie para armazenar JWT
COOKIE_NAME="access_token"

# Porta do servidor Express
PORT=4000

# ===== AMBIENTE =====
NODE_ENV="development"

# ===== AUTH (Next.js - se usar NextAuth) =====
AUTH_SECRET="outro-secret-aleatorio-se-usar-nextauth"

